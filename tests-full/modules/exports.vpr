// compile: {vprc} {input} -o {input}.ll

module sample
{
    export function foo()
    {
        return false;
    }

    export let int32 = sized_int(32);
    export struct abc {};

    // export with (T : type)
    // typeclass def {};
}

// vim: filetype=cpp
